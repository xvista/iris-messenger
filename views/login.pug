doctype html
html
	head
		meta(charset="utf-8")
		title Iris Messenger
		link(rel="stylesheet", href="css/bootstrap.min.css")
		link(rel="stylesheet", href="css/login.css")
	body
		div.container
			form.form-signin(action="/login", method="post")
				a(href="/")
					img(src="img/iris-logo.png")
				input.form-control.first(type="text", name="username", placeholder="Username", autofocus, required)
				input.form-control.last(type="password", name="password", placeholder="Password", required)
				button.btn.btn-lg.btn-primary.btn-block Login
				p.join
					| Don't have account? 
					a(href="/signup") Join us!
				if message != ''
					div.alert.alert-danger #{message}